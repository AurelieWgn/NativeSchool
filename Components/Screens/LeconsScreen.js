import { SchoolContext } from "../../Providers/SchoolProvider";
import { SafeAreaView } from "react-native-safe-area-context";
import {
  StyleSheet,
  Text,
  View,
  TouchableOpacity,
  FlatList,
} from "react-native";
import React, { useContext } from "react";

export const LeconsScreen = ({ navigation }) => {};
